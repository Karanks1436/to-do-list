import React,{useState}from'react';import{Alert,ScrollView,Text}from'react-native';import*as DocumentPicker from'expo-document-picker';import*as FileSystem from'expo-file-system/legacy';import{Btn,Card,Header}from'../components/UI';import{s}from'../styles';import{uploadBase64Document}from'../firebase/firebaseService';
export default function DocumentsScreen({go,user}){const[busy,setBusy]=useState(false);const choose=async()=>{try{const r=await DocumentPicker.getDocumentAsync({copyToCacheDirectory:true});if(r.canceled)return;const f=r.assets[0];if(f.size>700*1024)throw new Error('File must be under 700 KB.');setBusy(true);const base64=await FileSystem.readAsStringAsync(f.uri,{encoding:'base64'});await uploadBase64Document({uid:user.uid,name:f.name,mimeType:f.mimeType||'application/octet-stream',base64});Alert.alert('Uploaded','Document saved in Firebase.')}catch(e){Alert.alert('Upload failed',e.message)}finally{setBusy(false)}};return <ScrollView contentContainerStyle={s.page}><Header title="My Documents" back={()=>go('profile')}/><Card><Text style={s.whiteTitle}>Collector verification documents</Text><Text style={[s.small,{marginVertical:12}]}>Maximum 700 KB. Stored as Base64 as requested.</Text><Btn disabled={busy} title={busy?'Uploading…':'Choose Document'} onPress={choose}/></Card></ScrollView>}
