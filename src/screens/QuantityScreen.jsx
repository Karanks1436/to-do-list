import React from'react';import{ScrollView,Text,TextInput}from'react-native';import{Btn,Card,Header}from'../components/UI';import{s}from'../styles';
export default function QuantityScreen({go,quantity,setQuantity,rate,material}){const total=(Number(quantity)||0)*(Number(rate)||0);return <ScrollView contentContainerStyle={s.page}><Header title="Quantity & Price" back={()=>go('result')}/><Card><Text style={s.whiteTitle}>{material?.name||'Material'}</Text><Text style={s.small}>Estimated quantity (kg)</Text><TextInput style={[s.input,{color:'#173a31',borderColor:'#ccd9d5',marginTop:8}]} keyboardType="decimal-pad" value={quantity} onChangeText={setQuantity}/><Text style={s.small}>Current monthly rate</Text><Text style={[s.whiteTitle,{fontSize:18}]}>₹{Number(rate||0).toFixed(2)} / kg</Text><Text style={[s.whiteTitle,{fontSize:28,marginVertical:16}]}>Estimated ₹{total.toFixed(0)}</Text><Btn title="Find Nearby Collectors" onPress={()=>go('collectors')}/></Card></ScrollView>}
