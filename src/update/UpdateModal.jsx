import React,{useState}from'react';import{Alert,Linking,Modal,Text,TouchableOpacity,View}from'react-native';import*as Updates from'expo-updates';import{Btn}from'../components/UI';import{C}from'../theme';
export default function UpdateModal({update,onDismiss}){const[busy,setBusy]=useState(false);if(!update)return null;const install=async()=>{try{setBusy(true);if(update.type==='ota')await Updates.reloadAsync();else if(update.url&&await Linking.canOpenURL(update.url))await Linking.openURL(update.url);else Alert.alert('Download unavailable','The administrator has not configured a valid download URL.')}catch(e){Alert.alert('Update failed',e.message)}finally{setBusy(false)}};return <Modal transparent animationType="fade" visible onRequestClose={()=>!update.required&&onDismiss()}><View style={{flex:1,backgroundColor:'#000a',alignItems:'center',justifyContent:'center',padding:24}}><View style={{width:'100%',maxWidth:420,backgroundColor:C.panel2,borderRadius:18,padding:22,borderWidth:1,borderColor:C.line}}><Text style={{fontSize:42,textAlign:'center'}}>⬆️</Text><Text style={{fontSize:21,fontWeight:'800',color:C.text,textAlign:'center',marginTop:8}}>{update.title}</Text><Text style={{color:C.muted,textAlign:'center',lineHeight:20,marginVertical:14}}>{update.message}</Text><Btn disabled={busy} title={busy?'Opening…':update.type==='ota'?'Restart & Update':'Download Update'} onPress={install}/>{!update.required&&<TouchableOpacity onPress={onDismiss} style={{padding:14}}><Text style={{color:C.muted,textAlign:'center'}}>Later</Text></TouchableOpacity>}</View></View></Modal>}
